@model IEnumerable<MvcEliminatorias.Models.fecha>

@{
    ViewBag.Title = "FechaList";
}

<h2>FechaList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fkIdE1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fkIdE2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fkIdE1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fkIdE2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
